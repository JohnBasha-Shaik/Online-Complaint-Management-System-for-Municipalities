server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/complaintdb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root

logging:
  level:
    com.municipal.complaint: DEBUG